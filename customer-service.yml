logging:
  level:
    com.skcomms.springcloud: DEBUG

server:
  port: 8090
  
eureka:
  instance:
    lease-renewal-interval-in-seconds: 1
    lease-expiration-duration-in-seconds: 2
  client:
    service-url:
      defaultZone: http://127.0.0.1:9000/eureka
